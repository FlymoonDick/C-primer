#include<iostream>

int main()
{
	//std::cout << "2\n\vM" << std::endl;
	//std::cout << '1' << std::endl;
	// 2.34
	/*
	int i = 0, & r = i;
	auto a = r;
	const int ci = i, & cr = ci;
	auto b = ci; // b-int
	auto c = cr; // c-int
	auto d = &i; //d-int指针
	auto e = &ci;//e -指向int的常量指针
	auto &g = ci;//g - int声明
	a = 42, b = 42, c = 42;
	std::cout << a<<b<<c<< std::endl;
	std::cout << d << e << g<< std::endl;
	return 0;
	*/


	//2.35
	//const int i = 42;//整型常量
	//auto j = i; //j-int
	//const auto& k = i;//整型常量声明
	//auto  *p = &i;//指向整型常量的指针
	//const auto j2 = i, & k2 = i;//j2 整型常量， k2 整型常量声明
	//std::cout << typeid(i).name() << std::endl;
	//std::cout << typeid(j).name() << std::endl;
	//std::cout << typeid(k).name() << std::endl;
	//std::cout << typeid(p).name() << std::endl;
	//std::cout << typeid(j2).name() << std::endl;//？
	//std::cout << typeid(k2).name() << std::endl;//？
	//return 0;

	//2.42

}