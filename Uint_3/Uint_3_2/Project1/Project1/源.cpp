#include <iostream>
#include"string"
#include<vector>
//#include "uint3_test1.h"
using namespace std;

int main()
{
	//3.20
	//vector<int> v;
	//int n;
	//while (cin >> n)
	//{
	//		v.push_back(n);//crtl+z
	//}
	//if (v.size() == 0)
	//{
	//		return -1;
	//}
	//cout << "sum" << endl;
	//for (decltype(v.size()) i = 0; i < v.size() - 1; i += 2)
	//{
	//	cout << v[i] + v[i + 1] << " ";
	//	if ((i + 2) % 10 == 0);
	//	cout << endl;
	//}
	//if (v.size() % 2 != 0)
	//	cout << v[v.size() - 1];
	//return 0;

	//3.16
	/*string s;
	vector<string> v1;
	while (cin >> s)
	{
		v1.push_back(s);
	}
	for (auto &c : v1)
	{
		for (auto &c1 : c)
		{
			c1 = toupper(c1);
		}
		cout << c << endl;
	}
	return 0;*/

	//3.21
	//vector<string> v;
	//string n;
	//while (cin >> n)
	//{
	//	v.push_back(n);
	//}
	//for (auto it = v.begin(); it != v.end() && !it->empty(); it++)
	//{
	///*	for (auto& c : v)
	//	{
	//		for (auto& c1 : c)
	//			c1 = toupper(c1);
	//	}*/
	//	for (auto it2 = it->begin(); it2 != it->end(); it2++)
	//	{
	//		*it2 = toupper(*it2);
	//	}
	//	cout << *it << endl;
	//}
	//return 0;

	//3.23
	//vector<int> a(10);
	//int i;
	//for (auto it1 = 0; it1 != a.size(); it1++)
	//{
	//	cin >> i;
	//	a[it1] = i;
	//}

	//for(auto it = a.begin(); it != a.end(); it++)
	//{
	//	cout << *it *2<< endl;
	//}
	//return 0;


}